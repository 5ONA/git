const seed=[['SONA',8420],['ARJUN',7910],['MAYA',7250],['RAHUL',6810],['PLAYER',5930]];
export function getScores(){try{return JSON.parse(localStorage.getItem('dont-look-scores'))||seed.map(([name,score])=>({name,score}))}catch{return seed.map(([name,score])=>({name,score}))}}
export function saveScore(result){const scores=getScores(); scores.push({name:result.name||'PLAYER',score:result.score,time:result.time,hits:result.hits,shots:result.shots,misses:result.misses,accuracy:result.accuracy,combo:result.combo,lives:result.lives}); scores.sort((a,b)=>b.score-a.score); try{localStorage.setItem('dont-look-scores',JSON.stringify(scores.slice(0,10)))}catch{} }
export function clearScores(){try{localStorage.setItem('dont-look-scores','[]')}catch{}}
export function renderLeaderboard(root){const scores=getScores(); root.innerHTML=scores.slice(0,10).map((item,index)=>`<div class="leader-row"><strong>${String(index+1).padStart(2,'0')}</strong><span>${item.name}<small>${item.hits ?? 0} HITS - ${item.accuracy ?? 0}% ACC</small></span><b>${item.score}</b></div>`).join('')}
